import { Module } from '@nestjs/common';

// TODO: Module that I want to develop to handle image uploads ( probably a s3 bucket ) and processing.
// Relation to the entity that owns the image will be handled using polymorphic relations.
@Module({})
export class ImageModule {}
